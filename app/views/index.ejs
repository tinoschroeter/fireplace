<!DOCTYPE html>
<html lang="de">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>fireplace</title>
    <style>
      video.fullscreen {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
    </style>
  </head>
  <body>
    <% if (ready) { %> 
    <video src="fireplace.mp4" autoplay class="fullscreen">
      <% } else { %>
      <h1><%= download.percent %>% <%= download.eta %> time left<h/>
        <% } %>
        <script>
          window.addEventListener("keypress", (e) => console.log(e));
        </script>
  </body>
</html>
